#include<bits/stdc++.h>
using namespace std; 
int main(){
    int i,n,d,r=0,base=1;
    cin>>n;
    while(n!=0){
    d=n%10;
    r=r+(base*d);
    base=2*base;
    n=n/10;
    }
    cout<<"decimal no:"<< r;
    return 0;
}
